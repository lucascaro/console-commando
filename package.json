{
  "name": "console-commando",
  "version": "6.0.4",
  "description": "A tool for creating console commands",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "clean": "rm -rf lib/*",
    "compile": "npm run clean && tsc -p .",
    "docs": "rm -rf docs && typedoc --out docs src && mkdir -p versions/`npm run --silent version` && cp -r docs/* $_ && mv versions docs",
    "lint": "eslint src/**/*.ts",
    "prepublishOnly": "npm run test && npm run compile && npm run rollup",
    "prerelease": "npm run compile && npm run test && npm version prerelease && git push --follow-tags",
    "release": "npm run compile && npm run test && npm version patch && git push --follow-tags",
    "rollup": "rollup -c rollup.config.js",
    "test-watch": "mocha --require ts-node/register --watch-extensions ts,tsx 'test/**/*.{ts,tsx}'",
    "test": "jest",
    "version": "node -pe 'require(\"./package.json\").version'",
    "watch": "tsc -w -p ."
  },
  "author": "Lucas Caro <lucascaro@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:lucascaro/console-commando.git"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/jest": "^24.0.18",
    "@types/minimist": "^1.1.29",
    "@types/node": "^12.7.5",
    "@typescript-eslint/eslint-plugin": "^2.2.0",
    "@typescript-eslint/parser": "^2.2.0",
    "eslint": "^6.3.0",
    "eslint-config-prettier": "^6.3.0",
    "eslint-plugin-prettier": "^3.1.0",
    "jest": "^24.9.0",
    "mock-stdin": "^0.3.1",
    "prettier": "^1.18.2",
    "rollup": "^1.21.2",
    "rollup-plugin-commonjs": "^10.1.0",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-terser": "^5.1.2",
    "rollup-plugin-typescript2": "^0.24.2",
    "ts-jest": "^24.1.0",
    "ts-node": "^8.3.0",
    "tslib": "^1.10.0",
    "typedoc": "^0.15.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "immutable": "^4.0.0-rc.12",
    "minimist": "^1.2.0",
    "stdio-mock": "^1.1.0"
  }
}
